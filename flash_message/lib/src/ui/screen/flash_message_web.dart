import 'package:flutter/material.dart';

class FlashMessageWeb extends StatelessWidget {

  const FlashMessageWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    backgroundColor: Theme.of(context).colorScheme.surface,
    body: Container(),
  );
  }}