import 'package:flutter/material.dart';

class MusicPlayerWeb extends StatelessWidget {

  const MusicPlayerWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    backgroundColor: Theme.of(context).colorScheme.surface,
    body: Container(),
  );
  }}